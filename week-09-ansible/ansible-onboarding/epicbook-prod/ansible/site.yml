---
# EpicBook production-style deployment (Oluwagbade Odimayo).
# Roles run in dependency order: base system, then the web tier, then the application.

- name: Deploy EpicBook with Ansible roles
  hosts: web
  become: true
  roles:
    - common
    - nginx
    - epicbook
